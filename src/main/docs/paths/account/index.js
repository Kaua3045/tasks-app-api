const authPath = require('./auth-path')
const mePath = require('./me-path')
const addAccountPath = require('./add-account-path')
const confirmAccountPath = require('./confirm-account-path')
const resendAccountConfirmPath = require('./resend-account-confirm-path')

module.exports = {
  authPath,
  mePath,
  addAccountPath,
  confirmAccountPath,
  resendAccountConfirmPath
}