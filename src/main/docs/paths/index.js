const { authPath, mePath, addAccountPath } = require('./account')
const { addTaskPath, listTaskByIdPath } = require('./task')

module.exports = {
  authPath,
  mePath,
  addAccountPath,
  addTaskPath,
  listTaskByIdPath
}